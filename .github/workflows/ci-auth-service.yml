name: CI — auth-service

on:
  push:
    branches: [main, dev]
    paths: ['auth-service/**']
  pull_request:
    branches: [main, dev]
    paths: ['auth-service/**']

jobs:
  build:
    uses: ./.github/workflows/_gradle-build.yml
    with:
      service: auth-service
